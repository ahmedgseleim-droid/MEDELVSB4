name: Keep Alive

on:
  schedule:
    - cron: '0 12 */5 * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: |
          curl -f https://medelvsb3.onrender.com/health || curl -f https://medelvsb3.onrender.com/ || echo 'Ping completed'
