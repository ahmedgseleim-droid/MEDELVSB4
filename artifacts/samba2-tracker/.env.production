# Production environment for the Samba2 tracker frontend
# Set this to the shared backend or Supabase API URL used by both the staff tracker
# and the admin portal.
VITE_API_URL=https://medelvsb3.onrender.com

# Enable local browser fallback only when the backend is intentionally unavailable.
# Set this to true only for local development or debugging.
VITE_ALLOW_LOCAL_FALLBACK=false
